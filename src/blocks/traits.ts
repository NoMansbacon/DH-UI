/**
 * Traits code block processor
 *
 * Registers: ```traits (primary), ```ability (legacy alias)
 *
 * Displays Daggerheart trait cards with toggles for proficiency.
 * Shows base trait values plus bonuses.
 */
export { AbilityView } from "../components/traits";

